# PSC Consumer VPC Overview

A consumer VPC in PSC is a seprate VPC that consumes published services using PSC endpoints

* Uses PSC endpoints to enable workloads inside the consumer VPC to privately access services hosted in a separate producer VPC network with having the traffic leave Google's network or requiring public internet exposure