# PSC Producer VPC Overview

A producer VPC in PSC is a VPC that hosts and exposes one or more services through service attachments for consumption by other VPC networks

* Controls how the services are published 
* Managed allowed consumers
* Controls how the service is deployed, managed connections, configures access allowlists, and provides NAT and DNS configuration for private access through the PSC service attachments