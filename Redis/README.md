# Redis Overview

Redis is an open-source in-memory key-value database designed for fast data access

* Makes accessing data exceptionally fast by storing data primarily in RAM
* Uses various data structures for storing data